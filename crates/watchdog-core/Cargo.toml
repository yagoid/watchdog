[package]
name = "watchdog-core"
version.workspace      = true
edition.workspace      = true
license.workspace      = true
rust-version.workspace = true
description = "Shared types for the watchdog pipeline."

[dependencies]
serde_json = { workspace = true }
chrono     = { workspace = true }
